import './Work.scss'
import * as React from "react";

export class Work extends React.Component {
    render() {
        return (
            <div id={'work'} className={'WorkPage'}></div>
        );
    }
}
