import './Projects.scss'
import * as React from "react";

export class Projects extends React.Component {
    render() {
        return (
            <div id={'projects'} className={'ProjectsPage'}></div>
        );
    }
}
